/** Signed session cookie for Control Center operator auth. */
export const SESSION_COOKIE_NAME = "raqeem_cc_session";

/** Default session lifetime (7 days). */
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 7;
